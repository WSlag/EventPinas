rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /marketplaceProfiles/{profileType}/{profileId}/{ownerUid}/{purpose}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == ownerUid;
    }
  }
}
